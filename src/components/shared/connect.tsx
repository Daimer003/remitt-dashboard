
import { Button } from "@chakra-ui/react";

const Connect = () => {

  return (
    <Button
      h="30px"
      variant="buttonPrimary"
    >
      Conectar wallet
    </Button>
  );
};

export default Connect;
