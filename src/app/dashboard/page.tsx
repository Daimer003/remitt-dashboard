import Layout from "@/components/layout";
import { Box, Text } from "@chakra-ui/react";

const Dashboard = () => {
  return (
    <Layout>
      <Text>Dashboard</Text>
    </Layout>
  );
};

export default Dashboard;
